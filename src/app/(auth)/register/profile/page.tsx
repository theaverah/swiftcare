import { ProfileFlow } from "@/components/auth/profile/ProfileFlow";

export const metadata = { title: "Set Up Your Profile — SwiftCare" };

export default function ProfilePage() {
  return <ProfileFlow />;
}
